server:
  port: 8082

spring:
  application:
    name: student-service

  datasource:
    url: jdbc:mysql://localhost:3306/student_db
    username: root
    password: Gopal#413222

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    enabled: false
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka